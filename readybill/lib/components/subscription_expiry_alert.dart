import 'package:flutter/material.dart';

class SubscriptionExpiryAlert extends StatelessWidget {
  const SubscriptionExpiryAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}